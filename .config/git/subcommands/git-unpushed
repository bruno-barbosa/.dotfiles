#!/usr/bin/env bash

# Show commits not yet pushed to remote

branch=$(git branch --show-current 2>/dev/null)
if [[ -z "$branch" ]]; then
  echo "Not on a branch"
  exit 1
fi

echo "Commits not pushed to origin/$branch:"
git log origin/$branch..$branch --oneline --graph
